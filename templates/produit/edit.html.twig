{% extends 'baseadmin.html.twig' %}



{% block MainContent %}
    <h1>Edit Produit</h1>

    {{ include('produit/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('produit_back') }}">back to list</a>

    {{ include('produit/_delete_form.html.twig') }}
{% endblock %}
